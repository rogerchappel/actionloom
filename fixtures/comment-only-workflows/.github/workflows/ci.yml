name: Comment-only fields
# matrix: intentionally not configured
# cache: npm
permissions:
  contents: read
env:
  cache: npm
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    fail-fast: false
    steps:
      - uses: actions/setup-node@v5
      - run: npm test
