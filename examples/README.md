# Examples

- `node-ci.yml` is the conservative Node.js workflow shape generated by `actionloom generate node-ci`.
- `../fixtures/unsafe-workflows` intentionally includes risky patterns for tests and demos.
- `../fixtures/safe-workflows` shows the baseline workflow actionloom tries not to nag about.
